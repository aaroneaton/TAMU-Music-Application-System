TAMU Music Application System
-----------------------------


1. Copy files within the 'install' directory to their respective directory ( ex. install/application/config/database.php goes to /application/config/database.php )

2. Input configuration settings in those config files.

3. Create database for the application.

4. Run the included .sql script for your DB installation to create the proper
   tables and initial data.
